{
    "name": "medal",
    "version": "1.0.0",
    "description": "Worker for extracting direct video links from Medal.tv",
    "main": "src/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "Astrid",
    "license": "MIT",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "@tsndr/cloudflare-worker-router": "^1.1.1",
        "serverless-cloudflare-workers": "^1.2.0"
    }
}
